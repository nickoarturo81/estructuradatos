package Biblioteca;

public class Library {
    private Book[] libros;
    private int contarLibro;  // Why do we need this?
    
    public Library(int capacidad) {
        // Initialize array and counter
    }
}
